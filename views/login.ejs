
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Login</title>
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
  <div class="w-full min-h-screen bg-zinc-900 p-10 text-white flex flex-col items-center justify-center">
    <h2 class="text-3xl mb-5">Log in to your account</h2>
    <form action="/login" method="POST" class="flex flex-col gap-4 w-full max-w-sm">
      <input class="px-4 py-3 rounded bg-zinc-800 border border-zinc-600" type="email" name="email" placeholder="Email" required>
      <input class="px-4 py-3 rounded bg-zinc-800 border border-zinc-600" type="password" name="password" placeholder="Password" required>
      <button class="bg-blue-500 px-4 py-2 rounded text-white hover:bg-blue-600">Login</button>
    </form>
    <p class="mt-4 text-sm text-zinc-400">
      Don't have an account?
      <a href="/register" class="text-blue-400 underline">Create one</a>
    </p>
  </div>
</body>
</html>
