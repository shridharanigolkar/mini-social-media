<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Global Feed</title>
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body class="bg-zinc-900 text-white min-h-screen p-6">

  <div class="flex justify-between items-center mb-8">
    <h2 class="text-3xl font-semibold text-white">🌍 Global Feed</h2>
    <div class="flex gap-4">
      <a class="bg-yellow-500 text-black px-4 py-2 rounded-md text-sm font-medium hover:bg-yellow-400" href="/profile">Profile</a>
      <a class="bg-red-500 px-4 py-2 rounded-md text-sm font-medium hover:bg-red-400" href="/logout">Logout</a>
    </div>
  </div>


  <div class="flex flex-col items-center">
    <% posts.forEach(post => { %>
      <div class="bg-zinc-800 border border-zinc-700 p-5 mb-5 rounded-md w-full max-w-xl shadow-lg">
        <h3 class="text-blue-400 font-semibold mb-1">@<%= post.user.username %></h3>
        <p class="text-sm leading-snug"><%= post.content %></p>
        
        <div class="mt-3 flex items-center justify-between text-sm text-zinc-300">
          <span>❤️ <%= post.likes.length %> likes</span>
          <a href="/like/<%= post._id %>?redirect=/feed" class="text-blue-400 hover:underline">
            <%= post.likes.includes(currentUserId) ? "Unlike" : "Like" %>
          </a>
        </div>
      </div>
    <% }) %>
  </div>
</body>
</html>
