<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Register</title>
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
  <div class="w-full min-h-screen bg-zinc-900 p-10 text-white flex flex-col items-center justify-center">
    <h2 class="text-3xl mb-5">Create a new account</h2>
    <form action="/register" method="POST" class="flex flex-col gap-4 w-full max-w-sm">
      <input class="px-4 py-3 rounded bg-zinc-800 border border-zinc-600" type="text" name="name" placeholder="Name" required>
      <input class="px-4 py-3 rounded bg-zinc-800 border border-zinc-600" type="text" name="username" placeholder="Username" required>
      <input class="px-4 py-3 rounded bg-zinc-800 border border-zinc-600" type="email" name="email" placeholder="Email" required>
      <input class="px-4 py-3 rounded bg-zinc-800 border border-zinc-600" type="password" name="password" placeholder="Password" required>
      <button class="bg-green-500 px-4 py-2 rounded text-white hover:bg-green-600">Create Account</button>
    </form>
    <p class="mt-4 text-sm text-zinc-400">
      Already have an account?
      <a href="/login" class="text-blue-400 underline">Go to login</a>
    </p>
  </div>
</body>
</html>
