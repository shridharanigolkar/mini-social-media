<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
     <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 p-10 text-white">
        <div class="w-full flex justify-end">
          <a class="bg-red-500 rounded-md px-3 py-2 text-sm mb-5 inline-block" href="/logout">Logout</a>
        </div>
       <h5 class="mb-5">You can edit a post.</h5>
       <form method="post" action="/update/<%= post._id %>">
        <textarea class="resize-none bg-transparent border-2 border-zinc-800 rounded-md w-1/3" name="content" id="" placeholder="what's on your mind"><%= post.content %></textarea>
        <input class="px-3 w-40 py-2 text-sm bg-yellow-500 text-black block rounded-md mt-2" type="submit" value="update post">
       </form>
    </div>
</body>
</html>